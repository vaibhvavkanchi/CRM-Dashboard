/**
 * Allowed lifecycle states for lead record.
 * @type {string[]}
 */
export const LEAD_STATUS = ["new", "contacted", "qualified", "won", "lost"];

/**
 * Allowed acquisition sources for lead record.
 * @type {string[]}
 */
export const LEAD_SOURCE = ["website", "referral", "cold"];
