/**
 * Lead statuses used by frontend forms, filters, and analytics labels.
 * @type {string[]}
 */
export const LEAD_STATUS = ["new", "contacted", "qualified", "won", "lost"];

/**
 * Lead sources exposed in frontend forms and filters.
 * @type {string[]}
 */
export const LEAD_SOURCE = ["website", "referral", "cold"];
